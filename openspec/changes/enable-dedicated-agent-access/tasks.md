## 1. Политика инструментов и контейнерная граница

- [x] 1.1 Перевести шаблон `config/openclaw.json` на `tools.profile: "full"`, удалить конфликтующие `alsoAllow`, `fs.workspaceOnly` и `deny`, обновить русские комментарии и проверить файл через `node scripts/validate-config.mjs`.
- [x] 1.2 Удалить передачу всего `.env` через `env_file` из `compose.yaml`, сохранить явный allowlist необходимых OpenClaw-переменных и проверить результат командами `docker compose --env-file .env.example config --quiet` и `docker compose --env-file .env.example config`.
- [x] 1.3 Расширить `scripts/validate.sh` проверками полного профиля, отсутствия глобальных запретов и отсутствия `env_file` у OpenClaw-сервисов; убедиться, что скрипт выявляет возврат к ограниченной политике или передачу всего `.env`.

## 2. Миграция существующего persistent state

- [x] 2.1 Добавить идемпотентный `scripts/enable-agent-access.sh`, который точечно включает профиль `full`, удаляет старые конфликтующие ключи, сохраняет остальные настройки, валидирует конфигурацию и применяет её к Gateway; проверить первый и повторный запуск на временной тестовой конфигурации.
- [x] 2.2 Добавить Make-цель для одной команды миграции и включить новый скрипт в обязательные проверки проекта; проверить вызов через `make` и синтаксис всех shell-скриптов.

## 3. Русская документация

- [x] 3.1 Обновить `README.md`: описать полный доступ агента, рекомендовать отдельную заменяемую машину без посторонних данных/сервисов, указать требования к SSH, pairing, backup и показать одну команду миграции существующей установки; проверить отсутствие противоречащих утверждений про запрет shell/cron.
- [x] 3.2 Согласовать `docs/` и `policies/` с новой моделью доверенного агента: зафиксировать доступ к runtime/cron/state, контейнерные ограничения, видимость обязательных OpenClaw-токенов, недоступность restic/R2 credentials и сценарий восстановления; проверить поиском, что устаревшие ограничения удалены или явно помечены как прошлое поведение.

## 4. Итоговая проверка

- [x] 4.1 Выполнить `./scripts/validate.sh`, `git diff --check` и `openspec validate enable-dedicated-agent-access --type change --strict`; затем вручную проверить diff на отсутствие секретов и незапланированных изменений.
- [x] 4.2 На тестовом развертывании проверить health, безопасную shell-команду в основной и изолированной сессиях, запись persistent-файла, создание и удаление тестовой cron-задачи, а также отсутствие R2/restic-переменных в окружении контейнера.
