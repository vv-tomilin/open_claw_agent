{
  // Этот файл является исходным шаблоном. prepare-host.sh копирует его в persistent state.
  gateway: {
    mode: "local",
    bind: "lan",
    auth: {
      mode: "token",
      token: "${OPENCLAW_GATEWAY_TOKEN}",
    },
    controlUi: {
      allowedOrigins: ["${OPENCLAW_CONTROL_UI_ORIGIN}"],
    },
  },

  agents: {
    defaults: {
      workspace: "/home/node/.openclaw/workspace",
      model: {
        primary: "${PRIMARY_MODEL}",
        fallbacks: [],
      },
      utilityModel: "${UTILITY_MODEL}",
      models: {
        "openrouter/*": {},
      },
      subagents: {
        model: "${UTILITY_MODEL}",
      },
    },
  },

  channels: {
    telegram: {
      enabled: true,
      botToken: "${TELEGRAM_BOT_TOKEN}",
      dmPolicy: "pairing",
      groupPolicy: "disabled",
    },
  },

  // Агент может читать веб-источники, вести память, изменять только свой workspace,
  // отправлять сообщения и создавать изолированные подзадачи. Shell и управление
  // Gateway запрещены.
  tools: {
    profile: "minimal",
    alsoAllow: [
      "group:fs",
      "group:web",
      "group:memory",
      "group:messaging",
      "sessions_spawn",
      "sessions_yield",
      "subagents",
    ],
    fs: {
      workspaceOnly: true,
    },
    deny: [
      "group:runtime",
      "gateway",
      "cron",
      "sessions_send",
    ],
  },

  commands: {
    restart: false,
  },
}
